# HN Em-Dash Density

Fetches the current Hacker News front page and ranks every post by its em-dash (—) density: the percentage of characters in the combined title + body text that are em-dashes.

## Usage

```
python3 hn_em_dash_density.py
```

No third-party dependencies — uses only the Python standard library.
